{% extends 'base.html.twig' %}

{% block title %}
    {{ parent() }} - Créer une personne
{% endblock %}

{% block body %}
    <h1 class="mt-5">Créer une personne</h1>

    {{ form_start(personForm) }}
    {{ form_widget(personForm) }}
    <button class="btn btn-primary" type="submit">Valider</button>
    {{ form_end(personForm) }}

{% endblock %}